# 004: `x -> x` 是空操作，无法作为控制流标记

**严重程度**: 中 — 尝试用自赋值做逻辑标记时无声失败

**现象**: 写了 `pos -> pos` 以为能让循环条件触发退出，但什么都没发生。
kvlang 的赋值语义中，`x -> x` 确实是 no-op（读 x 再写回 x）。但用户直觉期望它至少产生某种"已访问"的副作用。

**建议**: 不需要改变语义。在 lower/vet 阶段对 `x -> x` 给出 warning，提示可能误用。
